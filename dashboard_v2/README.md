# Indian REITs Dashboard — v2

Version 2 rebuild of the Indian REITs dashboard. Lives on the `v2` branch of
[indian-reits-dashboard](https://github.com/IshanMandlaus/indian-reits-dashboard).

**Status:** fresh rebuild in progress — starting from a clean slate.

The v1 app (for reference) lives in [`../dashboard/`](../dashboard) on `main`.

## Notes
- Large source-data inputs (Embassy, MindSpace, Brookfield, Nexus, KRT, Bagmane)
  remain local-only and gitignored; derived data/images are committed so the app
  works from a clone.
